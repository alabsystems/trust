# extractor-repairs-verified-2026-07-21 — P-PRIM-CMP + W16-foreign, measured

Fresh dumps from the repaired extractor (stage2 @ the P-PRIM-CMP + W16-foreign
landing, after the 9-attempt rebuild saga: env_mutation lint compliance pushed
to ay/ty/clean/trust-vc mains + the ty lockfile + a disk exhaustion).

**44/46 FULLY_FAITHFUL — the family that graded 0/34 in wave 2:**
- <iN/uN as Ord>::min/max leaves: the FOUR-GATE-authenticated P-PRIM-CMP
  sentinel restores the sentinel-select lane on fresh dumps (the forgeable-
  attribute channel stays closed).
- core::num saturating_add/sub leaves: dumped for the first time (W16 foreign),
  certify via the intrinsic lane.
- min_max/max_min/sat_add_sub two-call chains + cmp::min/max forwarders: the
  full cascade composes end-to-end.
